import { Link } from "react-router-dom";

const Welcome = () => {
    return (
        <main>
            <button> <Link to='/game'> Start new game </Link> </button>
        </main>)
};

export default Welcome;